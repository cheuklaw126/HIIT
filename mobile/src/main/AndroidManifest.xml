<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kenneth.hiit">

    <uses-feature android:name="android.hardware.camera" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <application
        android:name=".Global"
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity

            android:name=".StartActivity"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LoginActivity"
            android:theme="@style/AppTheme" />
        <activity android:name=".IndexActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".RegisterActivity" />
        <activity
            android:name=".frdActivity"
            android:label="@string/title_activity_frd"
            android:theme="@style/AppTheme" />
        <!-- activity android:name=".ExercisePage" / -->
        <activity android:name=".HistoryPage" />
        <activity android:name=".HistoryList" />
        <activity android:name=".VideoAllList" />
        <activity android:name=".AnalysisPage" />
        <activity android:name=".GainIndex" />
        <activity android:name=".TotalTime" />
        <activity android:name=".HeartRate" />
        <activity android:name=".CaloriesBurn" />
        <activity android:name=".PartyActivity" />
        <activity android:name=".CreatePartyActivity" />
        <activity
            android:name=".ChatActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".CameraActivity" />
        <!-- <activity -->
        <!-- android:name=".chattingActivity" -->
        <!-- android:label="@string/title_activity_chatting" -->
        <!-- android:theme="@style/AppTheme" -->
        <!-- android:windowSoftInputMode="adjustPan"> -->
        <!-- </activity> -->
        <activity android:name=".JoinPtyActivity" />
        <activity android:name=".PartyVideoView" />
        <activity android:name=".testvideo" />
        <activity android:name=".GameActivity"
            android:configChanges="orientation|keyboardHidden"
            />
        <activity android:name=".partyVdoActivity"></activity>
    </application>

</manifest>